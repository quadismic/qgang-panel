import { AppShell } from "@/components/AppShell";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";
export const dynamic="force-dynamic";
export default async function Profile(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const {data:p}=await s.from("profiles").select("display_name,handle,bio,role").eq("id",user.id).maybeSingle();const providers=(user.identities??[]).map(i=>i.provider);return <AppShell><section className="profileHero"><div className="profileAvatar">{(p?.display_name??"Q").slice(0,1).toUpperCase()}</div><div><span className="kicker">Q-GANG IDENTITY</span><h1>{p?.display_name??"Q-GANG Member"}</h1><p>@{p?.handle??"member"} · {p?.role??"member"}</p></div><a className="button" href="/settings">Profili Düzenle</a></section><section className="panel"><h2>Bağlı hesaplar</h2><div className="connections"><div><b>Google</b><span>{providers.includes("google")?"Bağlı":"Bağlanabilir"}</span></div><div><b>Discord</b><span>{providers.includes("discord")?"Bağlı":"Bağlanabilir"}</span></div><div><b>Steam</b><span>Yakında</span></div></div></section><section className="panel"><h2>Hakkında</h2><p className="muted">{p?.bio||"Henüz bio eklenmedi."}</p></section></AppShell>}