import { AppShell } from "@/components/AppShell";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";export const dynamic="force-dynamic";export default async function Manage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/fund/manage");const {data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(!p||!["founder","admin"].includes(p.role))redirect("/fund");return <AppShell><div className="pageHead"><div><span className="kicker">Q-PANEL · FINANCE</span><h1>Fon Yönetimi</h1></div></div><section className="panel"><h2>Yeni Hareket</h2><form className="createForm" action="/api/fund" method="post"><label>İşlem<select name="kind" required><option value="support">Destek</option><option value="expense">Harcama / Çekiliş</option><option value="adjustment">Düzeltme</option></select></label><label>Başlık<input name="title" maxLength={100} required placeholder="Örn. Eylül oyun çekilişi"/></label><label>Tutar<input name="amount" type="number" min="0.01" step="0.01" required placeholder="0,00"/></label><label>Kategori<input name="category" maxLength={60} placeholder="Çekiliş, etkinlik, destek…"/></label><label>Destekçi adı<input name="supporter_name" maxLength={80} placeholder="Sadece destek işlemlerinde"/></label><label><input name="is_anonymous" type="checkbox" value="true"/> Destekçiyi toplulukta anonim göster</label><label>Açıklama<textarea name="description" maxLength={500}/></label><button className="primary">Hareketi Kaydet</button></form></section></AppShell>}