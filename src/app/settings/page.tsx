import { AppShell } from "@/components/AppShell";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";export const dynamic="force-dynamic";
export default async function Settings({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/settings");const {data:p}=await s.from("profiles").select("display_name,handle,bio").eq("id",user.id).single();const q=await searchParams;return <AppShell right={false}><div className="pageHead"><div><span className="kicker">IDENTITY</span><h1>Profilini Düzenle</h1></div></div>{q.saved&&<div className="notice">Profil kaydedildi.</div>}{q.error&&<div className="notice">Bu kullanıcı adı kullanılamıyor veya bilgiler geçersiz.</div>}<form className="createForm" action="/api/profile" method="post"><label>Görünen ad<input name="display_name" defaultValue={p?.display_name??""} required minLength={1} maxLength={50}/></label><label>Kullanıcı adı<input name="handle" defaultValue={p?.handle??""} required minLength={3} maxLength={24} pattern="[a-z0-9_]+"/><small className="muted">3–24 karakter · küçük harf, rakam ve _</small></label><label>Bio<textarea name="bio" defaultValue={p?.bio??""} maxLength={280} rows={5}/></label><div className="formActions"><a className="button" href="/profile">Vazgeç</a><button className="primary" type="submit">Kaydet</button></div></form><section className="panel"><h2>Bağlı hesaplar</h2><p className="muted">Google aktif. Discord ve Steam bağlantılarını Connected Accounts aşamasında ekleyeceğiz.</p></section></AppShell>}