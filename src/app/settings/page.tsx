import Link from "next/link";import {AppShell} from "@/components/AppShell";import {createClient} from "@/lib/supabase/server";import {redirect} from "next/navigation";import "../settings.css";export const dynamic="force-dynamic";
export default async function Settings({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/settings");const {data:p}=await s.from("profiles").select("display_name,handle,bio,birthday_day,birthday_month,birthday_year,birthday_visibility").eq("id",user.id).single();const q=await searchParams;return <AppShell right={false}><main className="identitySettings"><section className="identitySettingsHero"><small>Q-GANG · KİMLİK</small><h1>Kimliğini Düzenle</h1><p>Rumuzun, profil anlatın ve kişisel görünürlük tercihlerin. Giriş hesaplarını değiştirmek için Hesap Merkezi'ni kullan.</p></section>{q.saved&&<div className="identitySaved">Kimlik güncellendi.</div>}{q.error&&<div className="identitySaved">Bilgiler geçersiz veya kullanıcı adı kullanılamıyor.</div>}<form className="identitySettingsForm" action="/api/profile" method="post"><section className="identityFormSection"><div className="identityFormLegend"><small>GÖRÜNEN KİMLİK</small><h2>Profil</h2></div><div className="identityFieldGrid"><label>Görünen ad<input name="display_name" defaultValue={p?.display_name??""} required maxLength={50}/></label><label>Q-GANG kullanıcı adı<input name="handle" defaultValue={p?.handle??""} required minLength={3} maxLength={24} pattern="[a-z0-9_]+"/></label><label className="full">Hakkında<textarea name="bio" defaultValue={p?.bio??""} maxLength={280} rows={5} placeholder="Kendinden, oynadığın oyunlardan veya aradığın ekipten söz et…"/></label></div><p className="identityFieldHint">Kullanıcı adı yalnızca küçük harf, rakam ve alt çizgi içerebilir. Profil adresin bu kullanıcı adına bağlıdır.</p></section><section className="identityFormSection"><div className="identityFormLegend"><small>KİŞİSEL ALAN</small><h2>Doğum günü</h2></div><div className="identityFieldGrid birth"><label>Gün<input name="birthday_day" type="number" min="1" max="31" defaultValue={p?.birthday_day??""}/></label><label>Ay<select name="birthday_month" defaultValue={p?.birthday_month??""}><option value="">Seç</option>{["Ocak","Şubat","Mart","Nisan","Mayıs","Haziran","Temmuz","Ağustos","Eylül","Ekim","Kasım","Aralık"].map((x,i)=><option key={x} value={i+1}>{x}</option>)}</select></label><label>Yıl · opsiyonel<input name="birthday_year" type="number" min="1900" max={new Date().getFullYear()} defaultValue={p?.birthday_year??""}/></label></div><div className="identityFieldGrid" style={{marginTop:11}}><label>Doğum günü görünürlüğü<select name="birthday_visibility" defaultValue={p?.birthday_visibility??"community"}><option value="community">Sadece topluluk üyeleri</option><option value="public">Herkes</option><option value="private">Gizli</option></select></label></div><p className="identityFieldHint">Yıl zorunlu değildir. Görünürlüğü tamamen kapatabilirsin.</p></section><div className="identityFormActions"><Link href="/account">← Hesap Merkezi</Link><div><Link className="button" href="/profile">Vazgeç</Link><button className="primary">Kimliği Kaydet</button></div></div></form></main></AppShell>}
