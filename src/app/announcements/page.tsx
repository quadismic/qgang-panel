import {revalidatePath} from "next/cache";import {brandTheme} from "@/config/brand-theme";import type {CSSProperties} from "react";import {redirect} from "next/navigation";import {AppShell} from "@/components/AppShell";import {createClient} from "@/lib/supabase/server";import {canManage} from "@/lib/roles";export const dynamic="force-dynamic";
async function publish(formData:FormData){"use server";const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/announcements");const {data:me}=await s.from("profiles").select("role").eq("id",user.id).single();if(!canManage(me?.role))return;const title=String(formData.get("title")||"").trim(),body=String(formData.get("body")||"").trim();if(!title||!body)return;await s.from("announcements").insert({title,body,category:String(formData.get("category")||"DUYURU"),priority:String(formData.get("priority")||"normal"),is_pinned:formData.get("pinned")==="on",created_by:user.id});revalidatePath("/announcements");revalidatePath("/")}
export default async function Announcements(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const [{data:items},{data:me}]=await Promise.all([s.from("announcements").select("id,title,body,category,priority,is_pinned,published_at").order("is_pinned",{ascending:false}).order("published_at",{ascending:false}).limit(50),user?s.from("profiles").select("role").eq("id",user.id).single():Promise.resolve({data:null})]);const list=items??[],manage=canManage(me?.role);return <AppShell right={false}><section className="decreeHero fortressScene" style={{"--fortress-bg":`url(${brandTheme.world.master})`,"--fortress-pos":brandTheme.world.views.decrees.position,"--fortress-size":brandTheme.world.views.decrees.size} as CSSProperties}><h1>Duyurular</h1><p>Karargâhtan topluluğa yayımlanan resmî bildirimler, kararlar ve önemli gelişmeler.</p><b>{list.length}<small>YAYIMLANMIŞ KAYIT</small></b></section><div className="decreeGrid"><section className="decreeLedger"><header><span>RESMÎ KAYIT</span><b>SON YAYINLAR</b></header>{list.length?list.map((a:any)=><article className={"decree "+a.priority} key={a.id}><aside><span>{a.is_pinned?"◆":"◇"}</span><time>{new Date(a.published_at).toLocaleDateString("tr-TR",{day:"2-digit",month:"2-digit",year:"numeric"})}</time></aside><div><small>{a.category} // {a.priority.toUpperCase()}</small><h2>{a.title}</h2><p>{a.body}</p></div></article>):<div className="decreeEmpty"><span>▤</span><h2>Karargâh sessiz.</h2><p>Henüz yayımlanmış bir Q-GANG duyurusu bulunmuyor.</p></div>}</section>{manage&&<aside className="decreeEditor"><span>YÖNETİM // YENİ YAYIN</span><h2>Duyuru yayımla</h2><form action={publish}><label>Tür<select name="category"><option>DUYURU</option><option>KARAR</option><option>ETKİNLİK</option><option>SİSTEM</option></select></label><label>Önem<select name="priority"><option value="normal">Normal</option><option value="important">Önemli</option><option value="critical">Kritik</option></select></label><label>Başlık<input name="title" required/></label><label>Metin<textarea name="body" required rows={8}/></label><label className="decreeCheck"><input type="checkbox" name="pinned"/> Sabitle</label><button>YAYIMLA →</button></form></aside>}</div></AppShell>}