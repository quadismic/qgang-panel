import {createClient} from "@/lib/supabase/server";import {redirect} from "next/navigation";export const dynamic="force-dynamic";export default async function Maintenance(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(user){const {data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(p?.role==="founder")redirect("/")}return <main className="maintenanceGate"><div className="maintenanceSeal">Q</div><small>Q-GANG · KARARGÂH</small><h1>Karargâh şu an bakımda.</h1><p>Sistem düzenleniyor. Erişim yeniden açıldığında buradan devam edebilirsin.</p></main>}