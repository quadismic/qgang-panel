import { NextResponse } from "next/server";import { createClient } from "@/lib/supabase/server";export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.redirect(new URL("/login?next=/control/community",req.url),303);const {data:me}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(!me||!["founder","admin"].includes(me.role))return NextResponse.redirect(new URL("/",req.url),303);const f=await req.formData();const target=String(f.get("target_id")??"");const action=String(f.get("action")??"");if(!target||!["activate","deactivate"].includes(action))return NextResponse.redirect(new URL("/control/community",req.url),303);const {data:targetProfile}=await s.from("profiles").select("id").eq("id",target).maybeSingle();if(!targetProfile)return NextResponse.redirect(new URL("/control/community",req.url),303);if(action==="activate"){const {data:existing}=await s.from("community_memberships").select("user_id").eq("user_id",target).maybeSingle();if(existing)await s.from("community_memberships").update({status:"active",ended_at:null,granted_by:user.id,updated_at:new Date().toISOString()}).eq("user_id",target);else await s.from("community_memberships").insert({user_id:target,status:"active",granted_by:user.id})}else await s.from("community_memberships").update({status:"inactive",ended_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("user_id",target);return NextResponse.redirect(new URL("/control/community",req.url),303)}