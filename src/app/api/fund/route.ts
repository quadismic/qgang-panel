import { NextResponse } from "next/server";import { createClient } from "@/lib/supabase/server";export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.redirect(new URL("/login?next=/fund/manage",req.url),303);const {data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(!p||!["founder","admin"].includes(p.role))return NextResponse.redirect(new URL("/",req.url),303);const f=await req.formData();const kind=String(f.get("kind")??"");const title=String(f.get("title")??"").trim();const amount=Number(f.get("amount"));const description=String(f.get("description")??"").trim();const category=String(f.get("category")??"").trim().slice(0,60);const supporter_name=String(f.get("supporter_name")??"").trim().slice(0,80);if(!["support","expense","adjustment"].includes(kind)||title.length<2||title.length>100||!Number.isFinite(amount)||amount<=0||description.length>500)return NextResponse.redirect(new URL("/fund/manage?error=validation",req.url),303);await s.from("fund_transactions").insert({kind,title,amount,description,category:category||null,supporter_name:kind==="support"?(supporter_name||null):null,is_anonymous:kind==="support"&&f.get("is_anonymous")==="true",created_by:user.id});return NextResponse.redirect(new URL("/fund",req.url),303)}