import { AppShell } from "@/components/AppShell";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";
export const dynamic="force-dynamic";
export default async function Create(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/create");const {data:fires}=await s.from("campfires").select("id,name").eq("is_active",true).order("name");return <AppShell right={false}><section className="utilityHero"><span className="kicker">Q-GANG · CREATE</span><h1>Kıvılcım Bırak</h1><p>Genel ateşe veya bir Campfire’a yeni bir paylaşım bırak.</p></section><form className="createForm" action="/api/posts" method="post"><label>Campfire<select name="campfire_id"><option value="">Q-GANG</option>{(fires??[]).map(f=><option value={f.id} key={f.id}>{f.name}</option>)}</select></label><label>Paylaşım<textarea name="body" required maxLength={2000} rows={8} placeholder="Ne düşünüyorsun?"/></label><div className="formActions"><button className="primary" type="submit">Paylaş</button></div></form></AppShell>}