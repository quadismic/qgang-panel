import Link from "next/link";import {AppShell} from "@/components/AppShell";import {QGIcon} from "@/components/QGIcon";import {createClient} from "@/lib/supabase/server";import {redirect} from "next/navigation";
export const dynamic="force-dynamic";
export default async function Create(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const devPreview=process.env.NODE_ENV!=="production";if(!user&&!devPreview)redirect("/login?next=/create");const [{data:fires},{data:profile}]=await Promise.all([s.from("campfires").select("id,name").eq("is_active",true).order("name"),user?s.from("profiles").select("display_name,handle").eq("id",user.id).maybeSingle():Promise.resolve({data:null})]);const name=profile?.display_name||user?.email?.split("@")[0]||(devPreview?"Quad":"Sen");const handle=profile?.handle?"@"+profile.handle:(devPreview?"@quadismic":"Q-GANG üyesi");return <AppShell right={false}><section className="sparkPage sparkCinematic"><div className="sparkBackdrop" aria-hidden="true"/><div className="sparkShade" aria-hidden="true"/>{!user&&devPreview&&<div className="devPreviewFlag"><span>DEV PREVIEW</span><p>Arayüz önizlemesi · gerçek gönderim kapalı.</p></div>}<header className="sparkHead"><div className="sparkCrumb">Q-GANG <i>›</i> KIVILCIM</div><Link href="/" className="sparkBack">← Ocağa dön</Link><span className="sparkOverline">BAZI DÜŞÜNCELER DÜNYAYI DEĞİŞTİRİR.</span><h1><em>Ateşe</em> ne bırakacaksın?</h1><p>Bir düşünce. Bir soru. Bir keşif. Bir oyun anı.<br/>Burada her kıvılcım bir yere gider.</p></header><form className="sparkComposer" action={user?"/api/posts":undefined} method={user?"post":undefined}><div className="sparkTop"><div className="sparkIdentity"><span className="sparkAvatar">{name.slice(0,1).toUpperCase()}<i/></span><div><b>{name}</b><small>{handle}</small><span>Aynı ateşin insanlarından.</span></div></div><label className="sparkTarget"><span className="targetIcon"><QGIcon name="campfire"/></span><span className="targetCopy"><small>Nereye bırakıyorsun?</small><select name="campfire_id" aria-label="Paylaşım alanı"><option value="">Ocak · Genel Nabız</option>{(fires??[]).map(f=><option value={f.id} key={f.id}>{f.name}</option>)}</select></span></label></div><div className="sparkWriting"><textarea name="body" required maxLength={2000} rows={6} placeholder="Aklındakileri ateşe bırak…" aria-label="Paylaşım"/><span className="sparkCount">0/2000</span></div><div className="sparkTools"><div className="sparkToolset" aria-label="İçerik araçları"><button type="button" className="sparkTool dormant" title="Görsel desteği yakında"><QGIcon name="image"/><span>Görsel</span></button><button type="button" className="sparkTool dormant" title="Bağlantı desteği yakında"><QGIcon name="link"/><span>Bağlantı</span></button><button type="button" className="sparkTool dormant" title="Anket desteği yakında"><QGIcon name="poll"/><span>Anket</span></button></div><div className="sparkSendZone"><span>Kıvılcımını doğru yere bırak.<br/>Topluluk onu tamamlar.</span><button className="primary sparkSubmit" type={user?"submit":"button"} disabled={!user}><QGIcon name="hearth"/><b>{user?"Kıvılcımı Bırak":"Önizleme"}</b><QGIcon name="send"/></button></div></div></form><div className="sparkPrompts"><button type="button"><span><QGIcon name="video"/></span><div><b>Oyun Anı</b><small>“Bugün neler oynadın?”</small></div><i>›</i></button><button type="button"><span><QGIcon name="comment"/></span><div><b>Bir Düşünce</b><small>“Aklındaki fikri paylaş.”</small></div><i>›</i></button><button type="button"><span><QGIcon name="signals"/></span><div><b>Bir Soru</b><small>“Topluluğa danış.”</small></div><i>›</i></button><button type="button"><span><QGIcon name="people"/></span><div><b>Bir Keşif</b><small>“Bulduğun bir şeyi göster.”</small></div><i>›</i></button></div><footer className="sparkNote"><p>“Küçük bir kıvılcım, doğru insanla büyük bir ateş olur.”</p><span>Q-GANG</span></footer></section></AppShell>}