import {AppShell} from "@/components/AppShell";import {createClient} from "@/lib/supabase/server";import {redirect} from "next/navigation";export const dynamic="force-dynamic";export default async function Moderation(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/control/moderation");const {data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(!p||!["founder","admin"].includes(p.role))redirect("/");const {count}=await s.from("reports").select("*",{count:"exact",head:true});return <AppShell right={false}><section className="utilityHero"><span className="kicker">Q-GANG · MODERASYON</span><h1>Moderasyon Merkezi</h1><p>Yaptırım keyfî değil; gerekçeli, izlenebilir ve itiraza açık olacak.</p></section><div className="stats"><div><span>Raporlar</span><b>{count??0}</b></div><div><span>Model</span><b>5 Kademe</b></div><div><span>İtiraz</span><b>Açık</b></div></div><section className="panel"><span className="kicker">YAPTIRIM MERDİVENİ</span><h2>Uyarı → Kısıtlama → Susturma → Geçici Uzaklaştırma → Uzaklaştırma</h2><p className="muted">Her karar sebep, dayanak kural, süre, uygulayan yetkili, delil ve itiraz durumuyla kaydedilecek. Kaptan temel moderasyon; Vekilharç ağır yaptırımlar; Lider nihai yetki katmanıdır.</p></section></AppShell>}