import { AppShell } from "@/components/AppShell";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";export const dynamic="force-dynamic";export default async function Control(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/control");const {data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(!p||!["founder","admin"].includes(p.role))redirect("/");const [{count:users},{count:reports},{count:fires}]=await Promise.all([s.from("profiles").select("*",{count:"exact",head:true}),s.from("reports").select("*",{count:"exact",head:true}),s.from("campfires").select("*",{count:"exact",head:true}).eq("is_active",true)]);return <AppShell right={false}><section className="utilityHero"><span className="kicker">Q-GANG · YÖNETİM</span><h1>Yönetim Merkezi</h1><p>Topluluğun operasyon, üyelik ve güvenlik katmanı.</p></section><div className="stats"><div><span>Kullanıcılar</span><b>{users??0}</b></div><div><span>Raporlar</span><b>{reports??0}</b></div><div><span>Campfires</span><b>{fires??0}</b></div></div><div className="controlGrid"><a className="controlCard" href="/control/community"><span className="kicker">ÜYELİK</span><h2>Topluluk Üyeleri</h2><p>Üyelik statüsü ve iç alan erişimlerini yönet.</p></a><a className="controlCard" href="/fund/manage"><span className="kicker">FİNANS</span><h2>Topluluk Fonu</h2><p>Fon hareketleri, geçmiş ve düzeltme kayıtları.</p></a><div className="controlCard"><span className="kicker">MODERASYON</span><h2>Güvenlik</h2><p>Raporlar ve içerik yönetimi için operasyon alanı.</p></div><div className="controlCard"><span className="kicker">ERİŞİM</span><h2>Yetkiler</h2><p>Kurucu ve Yönetici erişimleri sunucu tarafında korunur.</p></div></div></AppShell>}