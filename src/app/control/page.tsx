import {AppShell} from "@/components/AppShell";import {createClient,getCurrentUser} from "@/lib/supabase/server";import {hasPermission} from "@/lib/access";import {redirect} from "next/navigation";
export const dynamic="force-dynamic";export const metadata={title:"Yönetim Merkezi · Q-GANG",description:"Q-GANG yönetim merkezi"};
const Icon=({type}:{type:"members"|"access"|"design"})=><span className={"controlModuleIcon controlModuleIcon-"+type} aria-hidden="true">{type==="members"?"◈":type==="access"?"⌘":"✦"}</span>;
export default async function Control(){const s=await createClient();const user=await getCurrentUser();if(!user)redirect("/login?next=/control");const [membersView,accessManage,designManage]=await Promise.all([hasPermission(user.id,"members.view"),hasPermission(user.id,"access.manage"),hasPermission(user.id,"design.manage")]);if(!membersView&&!accessManage&&!designManage)redirect("/");const {count:users}=membersView?await s.from("community_memberships").select("*",{count:"exact",head:true}).eq("status","active"):{count:null};const modules=[membersView&&{href:"/control/community",type:"members" as const,kicker:"KİMLİK & ROLLER",title:"Üyelik Merkezi",desc:"Topluluk üyeliklerini, kimlikleri ve rütbe işlemlerini yönet."},accessManage&&{href:"/control/access",type:"access" as const,kicker:"YETKİ MİMARİSİ",title:"Erişim Merkezi",desc:"Rütbelerin sistem yetkilerini canlı matris üzerinden yönet."},designManage&&{href:"/control/design",type:"design" as const,kicker:"GÖRÜNÜM",title:"Tasarım Merkezi",desc:"Marka, giriş ve temel görünüm değişkenlerini canlı önizlemeyle yönet."}].filter(Boolean) as {href:string;type:"members"|"access"|"design";kicker:string;title:string;desc:string}[];return <AppShell right={false}><main className="controlDashboard"><section className="controlDashboardHero"><div><span className="kicker">Q-GANG · YÖNETİM MERKEZİ</span><h1>İşletim Merkezi</h1><p>Topluluğun üyelik, yetki ve görünüm sistemlerini tek merkezden yönet.</p></div><div className="controlDashboardStatus"><span>SİSTEM DURUMU</span><b><i/> YÖNETİM AKTİF</b></div></section><section className="controlDashboardBar">{membersView&&<div><span>AKTİF ÜYE</span><b>{users??0}</b></div>}<div><span>ERİŞİLEBİLİR MODÜL</span><b>{modules.length}</b></div><div className="controlDashboardHint"><span>YÖNETİM KAPSAMI</span><b>Yetkilerine göre kişiselleştirilmiş</b></div></section><section className="controlModules">{modules.map(m=><a className="controlModuleCard" href={m.href} key={m.href}><div className="controlModuleTop"><Icon type={m.type}/><span className="controlModuleArrow">↗</span></div><div className="controlModuleCopy"><span className="kicker">{m.kicker}</span><h2>{m.title}</h2><p>{m.desc}</p></div><div className="controlModuleFoot"><span>MERKEZİ AÇ</span><b>→</b></div></a>)}</section></main></AppShell>}