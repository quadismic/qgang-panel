import { AppShell } from "@/components/AppShell";import { PostCard } from "@/components/PostCard";import { createClient } from "@/lib/supabase/server";import { notFound } from "next/navigation";export const dynamic="force-dynamic";
export default async function PublicProfile({params}:{params:Promise<{handle:string}>}){const {handle}=await params;const s=await createClient();const {data:p}=await s.from("profiles").select("id,display_name,handle,bio,role,created_at").eq("handle",handle.toLowerCase()).maybeSingle();if(!p)notFound();const [{count:fires},{data:raw}]=await Promise.all([s.from("campfire_members").select("*",{count:"exact",head:true}).eq("user_id",p.id),s.from("posts").select("id,body,created_at,campfire_id,campfires(name),reactions(count),comments(count)").eq("author_id",p.id).eq("is_removed",false).order("created_at",{ascending:false}).limit(30)]);const posts=(raw??[]).map((x:any)=>({id:x.id,author:p.display_name,handle:"@"+p.handle,body:x.body,campfire:x.campfires?.name,created_at:new Date(x.created_at).toLocaleDateString("tr-TR"),reactions:x.reactions?.[0]?.count??0,comments:x.comments?.[0]?.count??0}));return <AppShell><section className="profileHero"><div className="profileAvatar">{p.display_name.slice(0,1).toUpperCase()}</div><div><span className="kicker">Q-GANG MEMBER</span><h1>{p.display_name}</h1><p>@{p.handle} · {p.role}</p></div></section><section className="stats"><div><span>Paylaşım</span><b>{posts.length}</b></div><div><span>Campfire</span><b>{fires??0}</b></div><div><span>Katılım</span><b>{new Date(p.created_at).getFullYear()}</b></div></section><section className="panel"><h2>Hakkında</h2><p className="muted">{p.bio||"Henüz bio eklenmedi."}</p></section><div className="sectionTitle"><h2>Paylaşımlar</h2><span>{posts.length}</span></div><div className="feed">{posts.length?posts.map((x:any)=><PostCard key={x.id} post={x}/>):<div className="empty"><b>Henüz paylaşım yok.</b><p>İlk kıvılcım burada görünecek.</p></div>}</div></AppShell>}