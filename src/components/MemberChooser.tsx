"use client";import {useMemo,useState} from "react";export function MemberChooser({people}:{people:any[]}){const [q,setQ]=useState(""),[v,setV]=useState("");const list=useMemo(()=>people.filter(p=>[p.display_name,p.handle].join(" ").toLocaleLowerCase("tr").includes(q.toLocaleLowerCase("tr"))).slice(0,8),[people,q]);return <div className="memberChooser"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="İsim veya @rumuz ara…" autoComplete="off"/><input type="hidden" name="target" value={v}/>{q&&<div className="memberChooserResults">{list.map(p=><button type="button" key={p.id} onClick={()=>{setV(p.id);setQ(p.display_name+" · @"+p.handle)}}><b>{p.display_name}</b><span>@{p.handle}</span></button>)}</div>}</div>}