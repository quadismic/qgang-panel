"use client";
const tl=(n:number)=>new Intl.NumberFormat("tr-TR",{style:"currency",currency:"TRY"}).format(n);
export function TreasurySnapshot({balance,support,expense,last}:{balance:number;support:number;expense:number;last?:{title:string;amount:number;kind:string}|null}){return <button type="button" className="treasurySnapshot" onClick={()=>window.dispatchEvent(new CustomEvent("qgang:treasury"))}><div><span className="kicker">Q-GANG · HAZİNE</span><h2>{tl(balance)}</h2><p>Ortak kaynağın yaşayan kaydı.</p></div><div className="treasurySnapshotMeta"><span><small>Giriş</small><b>+ {tl(support)}</b></span><span><small>Çıkış</small><b>− {tl(expense)}</b></span>{last&&<span className="treasuryLast"><small>Son hareket</small><b>{last.kind==="expense"?"−":"+"}{tl(Number(last.amount))} · {last.title}</b></span>}</div><em>Hazineyi aç →</em></button>}