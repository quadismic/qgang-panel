"use client";
import {useState} from "react";
export function MemberPurgeControl({id,handle,name}:{id:string;handle:string;name:string}){const [open,setOpen]=useState(false),[value,setValue]=useState("");const valid=value.trim().toLowerCase()===handle.toLowerCase();return <><button className="danger" type="button" onClick={()=>{setValue("");setOpen(true)}}>KULLANICIYI SİL</button>{open&&<div className="purgeModalBackdrop" role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><section className="purgeModal" role="dialog" aria-modal="true" aria-labelledby={"purge-"+id}><small>GERİ DÖNDÜRÜLEMEZ İŞLEM</small><h2 id={"purge-"+id}>{name} hesabını sil</h2><p>Bu işlem hesabı kalıcı olarak siler. Doğrulamak için <b>@{handle}</b> kullanıcı adını yaz.</p><form action="/api/members/purge" method="post"><input type="hidden" name="target_id" value={id}/><label>Kullanıcı adı<input name="confirm_handle" value={value} onChange={e=>setValue(e.target.value)} placeholder={"@"+handle} autoComplete="off" required/></label><div className="purgeModalActions"><button type="button" onClick={()=>setOpen(false)}>VAZGEÇ</button><button className="danger" type="submit" disabled={!valid}>KALICI OLARAK SİL</button></div></form></section></div>}</>}