"use client";
import {useEffect,useRef,useState} from "react";
export function PostMenu({postId,isOwner=false}:{postId:string;isOwner?:boolean}){const [open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{function close(e:MouseEvent){if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)}document.addEventListener("mousedown",close);return()=>document.removeEventListener("mousedown",close)},[]);return <div className="postMenu" ref={ref}><button type="button" className="postMore" onClick={()=>setOpen(v=>!v)} aria-expanded={open} aria-label="Gönderi seçenekleri">•••</button>{open&&<div className="postMenuPanel">{isOwner?<><a href={`/posts/${postId}`}>Gönderiyi aç</a><form action="/api/posts/manage" method="post"><input type="hidden" name="post_id" value={postId}/><input type="hidden" name="action" value="delete"/><button className="danger" type="submit">Kıvılcımı kaldır</button></form></>:<><a href={`/posts/${postId}`}>Gönderiyi aç</a><form action="/api/reports" method="post"><input type="hidden" name="post_id" value={postId}/><button type="submit">Bildir</button></form></>}</div>}</div>}
