import Link from "next/link";import { createClient } from "@/lib/supabase/server";export async function MobileNav(){const s=await createClient();const {data:{user}}=await s.auth.getUser();let unread=0;if(user){const {count}=await s.from("notifications").select("*",{count:"exact",head:true}).eq("user_id",user.id).is("read_at",null);unread=count??0}return <nav className="mobileNav"><Link href="/">Home</Link><Link href="/search">Keşfet</Link><Link className="create" href="/create">+</Link><Link href="/campfires">Ateşler</Link><Link className="mobileSignal" href={user?"/notifications":"/login"}>{unread>0?<><span className="mobileBadge">{unread>99?"99+":unread}</span>Signals</>:"Profil"}</Link></nav>}