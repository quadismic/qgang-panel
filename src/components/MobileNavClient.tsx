"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {QGIcon} from "@/components/QGIcon";
const core=[["/rules","rules","Kurallar"],["/members","community","Topluluk"],["/penalties","discipline","Disiplin"]] as const;
export function MobileNavClient({moreHref,moreLabel}:{unread:number;moreHref:string;moreLabel:string}){const path=usePathname();const items=[...core,[moreHref,moreHref==="/control"?"control":moreHref==="/budget"?"treasury":"identity",moreLabel] as const];return <nav className="mobileNav hubMobile" aria-label="Q-GANG mobil menü"><Link className={path==="/"?"active":""} href="/"><i><QGIcon name="home"/></i><span>Karargâh</span></Link>{items.map(([href,icon,label])=><Link key={href} className={path.startsWith(href)?"active":""} href={href}><i><QGIcon name={icon}/></i><span>{label}</span></Link>)}</nav>}