"use client";import Link from "next/link";import {usePathname} from "next/navigation";export function MobileNavClient({unread,moreHref,moreLabel}:{unread:number;moreHref:string;moreLabel:string}){const path=usePathname(),a=(h:string)=>h==="/"?path==="/":path.startsWith(h);return <nav className="mobileNav" aria-label="Q-GANG mobil navigasyon"><Link className={a("/")?"active":""} href="/"><i>⌂</i><span>Ana Sayfa</span></Link><Link className={a("/search")?"active":""} href="/search"><i>⌕</i><span>Keşfet</span></Link><Link className="create" href="/create" aria-label="Yeni paylaşım">+</Link><Link className={"mobileSignal "+(a("/notifications")?"active":"")} href="/notifications">{unread>0&&<span className="mobileBadge">{unread>99?"99+":unread}</span>}<i>◆</i><span>Signals</span></Link><Link className={a(moreHref)?"active":""} href={moreHref}><i>{moreLabel==="Yönetim"?"⌘":moreLabel==="Fon"?"◇":"◎"}</i><span>{moreLabel}</span></Link></nav>}