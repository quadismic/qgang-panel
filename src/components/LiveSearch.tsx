"use client";import Link from "next/link";import {useEffect,useRef,useState} from "react";type Results={people:any[];fires:any[]};export function LiveSearch(){const [q,setQ]=useState(""),[data,setData]=useState<Results>({people:[],fires:[]}),[open,setOpen]=useState(false),[busy,setBusy]=useState(false);const seq=useRef(0);useEffect(()=>{if(!q.trim()){setData({people:[],fires:[]});setOpen(false);return}const id=++seq.current;const t=setTimeout(async()=>{setBusy(true);try{const r=await fetch("/api/search?q="+encodeURIComponent(q));const j=await r.json();if(id===seq.current){setData(j);setOpen(true)}}finally{if(id===seq.current)setBusy(false)}},220);return()=>clearTimeout(t)},[q]);return <div className="liveSearch"><div className="liveSearchBox"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} onFocus={()=>q&&setOpen(true)} onKeyDown={e=>{if(e.key==="Escape")setOpen(false)}} placeholder="İnsan veya Kamp Ateşi ara…" aria-label="Q-GANG'da ara"/>{busy&&<i className="searchPulse"/>}</div>{open&&<div className="searchLayer"><div className="searchGroup"><span className="kicker">İNSANLAR</span>{data.people.map(p=><Link href={"/u/"+p.handle} key={p.handle}><span className="avatar">{p.display_name.slice(0,1).toUpperCase()}</span><span><b>{p.display_name}</b><small>@{p.handle}</small></span></Link>)}</div><div className="searchGroup"><span className="kicker">KAMP ATEŞLERİ</span>{data.fires.map(f=><Link href={"/campfires/"+f.slug} key={f.slug}><span className="avatar">🔥</span><span><b>{f.name}</b><small>{f.description||"Kamp Ateşi"}</small></span></Link>)}</div>{!data.people.length&&!data.fires.length&&!busy&&<div className="searchEmpty">Bu kıvılcımın izi henüz yok.</div>}</div>}</div>}