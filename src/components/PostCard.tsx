import type { FeedPost } from "@/lib/types";
export function PostCard({post}:{post:FeedPost}){return <article className="post"><header><div className="avatar">{post.author.slice(0,1)}</div><div><strong>{post.author}</strong><span>{post.handle} · {post.created_at}</span></div>{post.campfire&&<em>🔥 {post.campfire}</em>}</header><p>{post.body}</p><footer><form action="/api/reactions" method="post"><input type="hidden" name="post_id" value={post.id}/><button type="submit">♡ {post.reactions}</button></form><a className="button" href={"/posts/"+post.id}>◌ {post.comments}</a><button type="button">↗ Paylaş</button></footer></article>}