import type { FeedPost } from "@/lib/types";
export function PostCard({post}:{post:FeedPost}){const h=post.handle.replace(/^@/,"");return <article className="post"><header><a className="avatar" href={"/u/"+h}>{post.author.slice(0,1)}</a><div><a href={"/u/"+h}><strong>{post.author}</strong></a><a href={"/u/"+h}><span>{post.handle} · {post.created_at}</span></a></div>{post.campfire&&<em>🔥 {post.campfire}</em>}</header><p>{post.body}</p><footer><form action="/api/reactions" method="post"><input type="hidden" name="post_id" value={post.id}/><button type="submit">♡ {post.reactions}</button></form><a className="button" href={"/posts/"+post.id}>◌ {post.comments}</a><button type="button">↗ Paylaş</button></footer></article>}