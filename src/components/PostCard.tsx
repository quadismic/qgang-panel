import type { FeedPost } from "@/lib/types";import {QGIcon} from "./QGIcon";
export function PostCard({post}:{post:FeedPost}){const h=post.handle.replace(/^@/,"");return <article className="post pulsePost"><header><a className="avatar postAvatar" href={"/u/"+h}>{post.author.slice(0,1).toUpperCase()}</a><div className="postIdentity"><a href={"/u/"+h}><strong>{post.author}</strong></a><a href={"/u/"+h}><span>{post.handle} · {post.created_at}</span></a></div>{post.campfire&&<em className="postCampfire"><QGIcon name="campfire"/> {post.campfire}</em>}</header><p className="postBody">{post.body}</p><footer className="postActions"><form action="/api/reactions" method="post"><input type="hidden" name="post_id" value={post.id}/><button type="submit" aria-label="Tepki bırak"><QGIcon name="react"/><span>{post.reactions}</span></button></form><a href={"/posts/"+post.id} aria-label="Yorumları aç"><QGIcon name="comment"/><span>{post.comments}</span></a><button type="button" aria-label="Paylaş"><QGIcon name="share"/><span>Paylaş</span></button></footer></article>}