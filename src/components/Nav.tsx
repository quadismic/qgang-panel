import {NavClient} from "./NavClient";import {createClient} from "@/lib/supabase/server";
export async function Nav(){const s=await createClient();const {data:{user}}=await s.auth.getUser();let manage=false,identity:any=null;if(user){const {data:p}=await s.from("profiles").select("display_name,handle,role,avatar_url,qgang_era,qgang_seal").eq("id",user.id).maybeSingle();identity=p;manage=!!p&&["founder","admin"].includes(p.role)}return <NavClient manage={manage} identity={identity}/>}
